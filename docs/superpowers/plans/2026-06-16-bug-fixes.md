# [2026-06-16 14:45] Implementation Plan - Bug Fixes and Reliability Improvements

This plan addresses the bugs and deficiencies identified in `bug-report-20260616-143630.md`, incorporating findings from peer review.

## Phase 1: High Priority Bug Fixes

### 1. Fix TypeError in OS Breakdown
- **File:** `src/github_usage/report_actions.py`
- **Change:** Update `total_os[os_name] += os_millis` to `total_os[os_name] += os_millis[os_name]`.
- **Validation:** Add unit test for `show_actions_os_breakdown` or mock `get_actions_from_runs` to ensure it aggregates correctly.

### 2. Fix Workflow Minutes Over-counting
- **File:** `src/github_usage/billing.py`
- **Change:** `workflow_minutes[wf_name]` should accumulate each run's own billable millis (divided by 60000), rather than the ever-growing cumulative `total_minutes`.
- **Validation:** Add unit test for `get_actions_from_runs` with multiple runs for the same workflow.

### 3. Fix 403 Rate-Limit Retry
- **File:** `src/github_usage/api.py`
- **Change:** Read `Retry-After` from the HTTP response headers (`resp.getheader("Retry-After")`) instead of the JSON body. Add a max-retry counter to prevent infinite recursion.
- **Validation:** Mock a 403 response with a `Retry-After` header and verify the retry logic.

### 4. Prevent Crash on Missing Monthly Costs
- **File:** `src/github_usage/report_data.py:156`
- **Change:** Add a guard to check if `report_data.get("monthly_costs")` is `None` before attempting to access `["total"]["net"]` in `get_warning_state`.

## Phase 2: Medium Priority Fixes

### 5. URL Encoding for Query Parameters and Path Safety
- **File:** `src/github_usage/api.py:22-25`
- **Change:** Use `urllib.parse.urlencode` to build the query string and ensure proper joining of paths and query parameters (handling paths with trailing `?` or `&`).
- **Validation:** Verify that parameters with special characters are correctly encoded.

### 6. Robust Pagination and Error Handling
- **File:** `src/github_usage/api.py`
- **Change:** Check the `Link` header for "next" page. Propagate non-list error responses instead of breaking the loop silently.
- **Validation:** Test `get_all_pages` with various response types (empty, error, single page, multiple pages).

### 7. Safer Repo Metadata Extraction
- **File:** `src/github_usage/storage.py`
- **Change:** Use `.get()` for `repo["owner"]["login"]` and `repo["name"]`. Wrap per-repo processing in `try/except`.
- **Validation:** Test with malformed repo dictionaries.

### 8. Fix `check_user_scope` and Close HTTP Connections
- **Files:** `src/github_usage/auth.py` and `src/github_usage/api.py`, `src/github_usage/legacy_report.py`
- **Change:** Use `with` or `try/finally` to close `HTTPSConnection` instances across all files to prevent connection leaks. Improve scope check by checking `resp.status` and handling missing headers.
- **Validation:** Test with various token types and mock responses.

### 9. Consistent "No GitHub token found" Message
- **Files:** `src/github_usage/cli.py` and `src/github_usage/legacy_report.py`
- **Change:** Extract shared helper for printing the missing token message.
- **Validation:** Verify consistent output across subcommands.

### 10. Graceful Warning State when Actions are Skipped
- **File:** `src/github_usage/report_data.py`
- **Change:** Handle the case where Actions data is missing when `--warn-over` is used.
- **Validation:** Run with `--skip-actions --warn-over 80%` and verify no crash.

## Phase 3: Style and Quality Improvements (Low Priority)

### 11. Handle `SystemExit` Correctly
- **File:** `src/github_usage/cli.py`
- **Change:** Refactor `try/except SystemExit` by using argparse's `exit_on_error=False` (if Python >= 3.9) or catch only the specific expected cases instead of swallowing everything.

### 12. Refactor `report_summary.py`
- **File:** `src/github_usage/report_summary.py`
- **Change:** Split `show_final_summary` into smaller, testable functions.

### 13. Fix Precedence Bug and Division by Zero Risks
- **File:** `src/github_usage/report_summary.py`
- **Change:**
  - Fix the ternary precedence bug at line 235: `if user_minutes and top2_sum / user_minutes * 100 > 70:`
  - Add safeguards against `ZeroDivisionError` at line 43 (`total_gross`) and line 211.

### 14. Fix Storage Progress Bar Length Consistency
- **File:** `src/github_usage/report_summary.py:163`
- **Change:** Define a `storage_bar_len` variable instead of confusingly reusing `min_bar_len` for the unfilled portion calculation.

### 15. Better JSON Parse Failure Handling
- **File:** `src/github_usage/api.py`
- **Change:** Log or raise on 200 responses that aren't valid JSON.

### 16. Safe Integer Conversion for Sizes
- **File:** `src/github_usage/report_optional.py:44, 65`
- **Change:** `int()` crashes on `None` or `""`. Change logic to safely parse the size (e.g. `int(asset.get("size") or 0)`).

### 17. Rename `os_minutes` to `os_millis`
- **File:** `src/github_usage/billing.py` and `src/github_usage/report_actions.py`
- **Change:** Rename `os_minutes` to `os_millis` in `billing.py` to accurately reflect its content, keeping it consistent with how `report_actions.py:82` already expects and unpacks `os_millis`.

## Phase 4: Test Coverage
- Add unit tests for modules with zero coverage: `report_account.py`, `report_actions.py`, `report_products.py`, `report_summary.py`, `storage.py`.
- Add tests for edge cases identified in the bug report.
